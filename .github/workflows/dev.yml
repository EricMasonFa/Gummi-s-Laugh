name: CI

on:
  push:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Go environment
        uses: actions/setup-go@v2.0.3
        with:
          go-version: 1.13
      - uses: actions/checkout@v2
      - name: Build
        run: make bin
